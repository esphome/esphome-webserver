import "./esp-app";

// Inject so will work on V1 server.. doesn't preserve any 0.js script
// let el=document.body
// el.classList.add('mememe')
// el.innerHTML = "<esp-app></esp-app>";
//console.log(window.matchMedia('(prefers-color-scheme)'));